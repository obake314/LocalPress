node_modules/
dist/
build/
.cache/
.env*
!.env.example
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/

# 素材（アプリからは参照していない）。
# 事業計画書と参考サイトのキャプチャなので公開リポジトリには含めない。
src/imports/

# 収集結果は取得のたびに作り直すもので、ソースではない。
# GitHub Actions では artifact として残している。
collector/*/output/
